@*@model IEnumerable<ErrorLoggerModel.Log>*@

@{
    ViewBag.Title = "Index";
}

<br />
<br />
<div style="margin-left:auto;margin-right:auto">
    <div class="mytitle">
        Index
    </div>
</div>
<br />

@*<p>
    @using (Html.BeginForm(FormMethod.Post))
    {
        if ((ViewData["Applications"] != null))
        {
            @Html.DropDownList("Applications", "Select Application")
            <br /><br />
            <input type="submit" value="Select" formmethod="post" />
        }
        else
        {
        <p> No Applications mapped </p>
        }
    }
    </p>

    @if (Model != null)
    {
        if (Model.Count() > 0)
        {
            <table style="width:100%">
                <tr>
                    <td style="width:60%">
                        <table class="table">
                            <tr>
                                <th style="font-weight:bold">
                                    @Html.DisplayNameFor(model => model.type)
                                </th>
                                <th style="font-weight:bold">
                                    @Html.DisplayNameFor(model => model.timestamp)
                                </th>
                                <th style="font-weight:bold">
                                    @Html.DisplayNameFor(model => model.description)
                                </th>
                                <th></th>
                            </tr>

                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.type)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.timestamp)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.description)
                                    </td>
                                    <td>
                                        @Html.ActionLink("Details", "Details", new { id = item.LogId })
                                    </td>
                                </tr>
                            }

                        </table>
                    </td>
                    <td style="width:40%;align-content:center"><img src="@Url.Action("DispChart","User")" alt="Chart" /></td>
            </table>
        }
        else
        {
            <p>No Logs</p>
        }
    }*@
